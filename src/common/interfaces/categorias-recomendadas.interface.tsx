export interface Categorias {
    id: number
    name: string
    description: string
    created_at: string
    slug: string   
}

export interface CategoriasHeader {
    id: number
    name: string
    slug: string
}